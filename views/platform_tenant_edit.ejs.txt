cat > views/platform_tenant_edit.ejs << 'ENDOFFILE'
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>平台管理 - 编辑租户</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<div class="topbar">
  <div class="brand">🛠 平台管理后台</div>
</div>
<div class="container">
  <h1>编辑租户 · <%= tenant.tenant_code %></h1>
  <div class="card" style="max-width:520px">
    <% if (error) { %><div class="error"><%= error %></div><% } %>
    <form method="POST" action="/platform-admin/tenants/<%= tenant.id %>/edit">
      <div class="form-row">
        <label>租户名称</label>
        <input type="text" name="tenant_name" value="<%= tenant.name || tenant.tenant_name || '' %>" required>
      </div>
      <div class="form-row">
        <label>服务到期日期</label>
        <input type="date" name="expires_at" value="<%= tenant.expires_at || '' %>">
        <span class="muted" style="font-size:12px">留空 = 不限</span>
      </div>
      <div class="form-row">
        <label>账号数上限</label>
        <input type="number" name="max_users" min="1" value="<%= tenant.max_users || '' %>" placeholder="留空 = 不限">
      </div>
      <button class="btn" type="submit" style="width:100%">保存</button>
    </form>
  </div>

  <div class="card" style="max-width:520px; margin-top:20px">
    <h2 style="margin-top:0; font-size:16px">租户账号 · 重置密码</h2>
    <p class="muted" style="font-size:13px; margin-top:-6px">
      租户管理员忘记密码、又没有其他管理员账号可以互相重置时，在这里帮他直接重置。
    </p>
    <% if (pwError) { %><div class="error"><%= pwError %></div><% } %>
    <% if (!tenantUsers || tenantUsers.length === 0) { %>
      <p class="muted">未能读取到该租户的账号列表（租户库文件可能还没生成）。</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>用户名</th>
            <th>姓名</th>
            <th>角色</th>
            <th>状态</th>
            <th>重置密码</th>
          </tr>
        </thead>
        <tbody>
          <% tenantUsers.forEach(u => { %>
            <tr>
              <td><%= u.username %></td>
              <td><%= u.name %></td>
              <td><%= u.role === 'admin' ? '管理员' : '操作员' %></td>
              <td><%= u.active ? '正常' : '已禁用' %></td>
              <td>
                <form method="POST"
                      action="/platform-admin/tenants/<%= tenant.id %>/users/<%= u.id %>/reset-password"
                      style="display:flex; gap:6px; align-items:center">
                  <input type="password" name="new_password" placeholder="新密码，至少6位" minlength="6" required style="width:140px">
                  <button class="btn" type="submit" style="white-space:nowrap">重置</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </div>

  <p style="margin-top:14px"><a href="/platform-admin">← 返回租户列表</a></p>
</div>
</body>
</html>
ENDOFFILE

echo "写入完成"