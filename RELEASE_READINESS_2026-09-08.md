# 进销存 SaaS 第三轮审查 · 能否对外收费运营

**审查日期**：2026-09-08（晚）
**代码基线**：`eb0df68` + 工作区 5 个未提交改动（9-08 下午的方案 B 修复）
**审查角度**：不是再找代码 bug，而是回答一个问题——**现在让租户进来用、并且收钱，出事了我兜得住吗？**
**方法**：全量代码走查（app.js + 14 routes + 12 lib + 4 middleware + 2 utils + 35 ejs）+ 真实启动服务的端到端实测

> **2026-09-08 深夜更新**：P0-1（限流互锁）与 P0-3（库存修正）已按本报告方案修复并实测通过；
> 全量回归 102 项全过（套件已留档 `tests/regression.js`）。详见文末「修复与回归记录」。

---

## 一句话结论

**业务内核是可靠的，可以到"让熟人客户进来试用"的程度；但还不到"对外收费"的程度。**

分两档说：

| 档位 | 判定 | 说明 |
|---|---|---|
| 免费试用 / 熟人内测 | ✅ **可以放开** | 核心账务数字实测自洽，权限与越权防护有效 |
| 正式收费 | ❌ **还差 3 件事** | 数据零备份、登录会全平台互锁、库存错了没法改 |

理由：收费意味着客户把经营数据资产交给你保管。"没有备份"和"账面错了没法修正"这两条，
不是功能多少的问题，是**责任兜不住**的问题——真出了事只能赔钱或丢客户。

---

## 一、实测证据（这部分是好消息）

我真实起了一个服务实例，跑完整业务链路，不是纸面推演。

### 场景
租户建商品（成本 10 / 售价 20）→ 采购入 100 → 销售 60 并审核 → 退货 10 并审核。

### 核对结果

| 项目 | 系统输出 | 手算预期 | 结论 |
|---|---|---|---|
| 库存 | 50 瓶 | 100 − 60 + 10 = **50** | ✅ |
| 销售额 | ¥1000.00 | 1200 − 200 = **1000** | ✅ |
| 退货额 | ¥200.00 | 10 × 20 = **200** | ✅ |
| 应收金额 | ¥400.00 | 1200 − 600已收 − 200退货 = **400** | ✅ |
| 含应收毛利 | ¥500.00 | 60×10 − 10×10 = **500** | ✅ |
| 不含应收毛利 | ¥0.00 | 该单未结清，不计入 = **0** | ✅ |

**9-08 下午那次毛利口径重构（方案 B）真正生效了**，首页、销售列表、经营报表三处数字全部对齐，
没有出现"同一笔款两个页面两个数"的老毛病。

### 安全项实测

| 检查 | 结果 |
|---|---|
| 操作员访问他人销售单详情 | 403 ✅ |
| 操作员访问经营报表 | 403 ✅ |
| 操作员编辑商品 | 403 ✅ |
| 跨站 Origin 提交 POST | 403 ✅ |
| 全量 JS 语法检查 | 26 个文件全通过 ✅ |
| 模板 XSS（`<%-` 用法） | 仅用于 `include` 和已转义 `<` 的 JSON 注入，无裸输出 ✅ |

**结论：账务内核和权限边界这两块，可以放心。** 这是多租户 SaaS 最难做对的部分，当前代码做到了。

---

## 二、收费前必须解决的三件事

### P0-1 登录限流是全平台共用一个桶 —— 一个客户输错密码，全体锁 15 分钟

**实测确认，不是理论推测。**

```
用 X-Forwarded-For: 1.1.1.1 连续输错密码 10 次
再换 X-Forwarded-For: 9.9.9.9 用【正确密码】登录
→ HTTP 429  「登录尝试过于频繁，请 X 分钟后再试」
```

**根因**：`.env` 里 `TRUST_PROXY=0`（代码 `app.js:29`），Express 不解析 XFF，
`req.ip` 一律取 socket 地址。Docker 部署下所有请求都来自同一个反代地址，
于是 `middleware/rateLimit.js:27` 的限流 key `ip:path` 对**全平台所有租户**是同一个值。

**后果**：10 次失败 / 15 分钟的配额，是全体客户共享的。快消行业客户在手机和电脑多端登录，
输错密码极其常见——5 个客户各输错 2 次，全平台所有人 15 分钟登不进去。
对收费服务来说这是**必然发生**的事故，不是概率事件。

**修复**（两件都要做，二选一不够）：
1. `.env` 改 `TRUST_PROXY=1`，并**确认 Nginx Proxy Manager 会覆盖（而不是透传）客户端 XFF**。
2. 更稳妥：把限流 key 从 `ip:path` 改成 `ip:path:租户代码:用户名`（`middleware/rateLimit.js:26`）。
   这样即使 IP 解析失效，不同租户/不同账号之间也不互相牵连。

> 注意：改 `TRUST_PROXY=1` 后要复查 `lib/rateLimiter.js` 里依赖 `req.ip` 的短信限流
> （`trialAuth.js:35`）。反代配置正确的话是变准了；配置错了反而会被伪造 XFF 绕过。

---

### P0-2 数据零备份 —— 一次误删就是全部客户的数据

**确认：全项目没有任何备份机制。**

- 无备份脚本、无定时任务、无导出功能
- `docker-compose.yml:14` 只有 `./data:/app/data` 一个 bind mount，
  `platform.db`（租户总表）+ `sessions.db` + `data/tenants/*.db`（每个客户一个库）全在这一处
- 磁盘故障 / `rm -rf` 误操作 / 容器重建覆盖 = **全部客户数据不可恢复**

另外 `.dockerignore` 里把 `*.md` 排除了、`data` 也排除了，构建侧是干净的，这点没问题。

**修复**（必须做，工作量约半小时）：
- 加一个 `scripts/backup.sh`，用 `sqlite3 /path/x.db ".backup /backup/x-YYYYMMDD.db"`
  （**不要直接 `cp`**——项目开了 WAL，`cp` 出来的可能是不一致状态）
- 配 crontab 每天一次，异地保留（比如同步到对象存储或另一台机器）
- **务必先做一次恢复演练**，验证备份文件真能打开

建议顺手确认：服务器磁盘剩余空间、是否有快照能力。

---

### P0-3 库存错了没法改 —— 没有库存调整/盘点功能，而 P1-1 会制造这种错

这两个问题要连起来看，单独看都不算致命，**组合起来是死结**。

**(a) 系统没有任何"库存调整 / 盘点"入口**（已 grep 确认，全项目无此功能）。
出入库流水里有 `adjust` 类型，但那只是系统内部冲销用的，用户端没有操作界面。

**(b) 审计遗留的 P1-1，本轮实测复现成功**：

```
采购入 100 瓶          → 库存 100
销售 100 瓶并审核      → 库存 0    （采购的这批货已卖光）
客户退货 100 瓶并审核  → 库存 100  （这 100 瓶是客户退回的实物，不是采购来的）
删除那张采购单         → 库存 0    ← 错误：实物在仓库里躺着，账面却是 0
```

根因在 `routes/purchases.js:134-144`：删除校验是「当前库存 ≥ 采购量」，
但**当前库存不等于这批采购的货还在**——它可能来自完全不同的来源（退货入库 / 调拨入库）。

**(a) + (b) 的后果**：一旦触发，仓库里实物有货、系统显示 0，
后续所有销售单都被"库存不足"拦死。**而用户没有任何合法的修正手段**——
既没有盘点调整，也没法靠删单重录（采购单已经删了）。只能求你手工改数据库。

**修复**（按性价比排序）：
1. **低成本止血**（约 20 分钟）：`purchases.js` 删除前加一条检查——
   若该商品在该仓库存在 `sale_return` 或 `transfer_in` 类型的流水，直接拒绝删除并给出可读提示。
2. **同时补一个"库存调整"页面**（约 1-2 小时）：管理员可指定商品+仓库+调整后数量+原因，
   写一条 `adjust` 流水。这是进销存的刚需功能，快消客户盘点时天天要用，
   也是所有库存类异常的最终自救手段。
3. 长期：采购单改为"作废冲红"而非物理删除（改动较大，可后置）。

> 补充：审计里提过的 P1-2（退货仓库可随意选、退货数量无上限）仍未修。
> 老胡之前判断影响有限，我复核后同意——有意为之的自由录入 + 可人工核对，风险可控。
> 但**如果补了库存调整功能，这几条的严重性会进一步下降**，因为错了能改回来。

---

## 三、建议一并处理（非阻塞）

| 项 | 说明 | 工作量 |
|---|---|---|
| `COOKIE_SECURE=false` | 站点若是 HTTPS（`erp.yayaagent.com`），必须改成 `true`，否则 session cookie 可被明文劫持 | 1 分钟 |
| 无安全响应头 | 全站缺 `X-Frame-Options` / `X-Content-Type-Options` / `Referrer-Policy`，可被点击劫持。加 4 行响应头即可 | 10 分钟 |
| 状态更新无 CAS 保护 | `UPDATE ... SET status='approved' WHERE id=?` 没带 `AND status='submitted'`（`sales.js:428`、`transfers.js:217`、`returns.js:362`）。当前单进程安全，将来上多副本会重复扣库存 | 20 分钟 |
| `order_date` 无格式校验 | 非法日期会让单据从月度统计里永久消失（`dashboard.js` 的 `strftime` 返回 NULL）。加个正则 | 10 分钟 |
| 试用注册泄露手机号 | `trialAuth.js:24` 在发码前就返回"已开通"，可批量探测你的客户名单。把 409 挪到验证之后 | 10 分钟 |
| `getTenantUsersSafe` 句柄泄漏 | `platformAdmin.js:252-261`，`prepare` 抛错时 db 未 close。影响很小但顺手可修 | 2 分钟 |

---

## 四、上线前 checklist

按这个顺序做完就可以收费了（除标注外，均已在本轮完成）：

- [x] 限流双桶改造 + 实测不同租户/账号互不影响（2026-09-08 深夜完成）
- [x] `purchases.js` 删除前拒绝"存在退货/调拨入库流水"的情况（完成 + 实测翻车链路被拦）
- [x] 库存调整/盘点页面（完成 + 实测调整/流水/权限全通过）
- [x] 修复回归中发现的 `isAdmin` 未传导致库存页 500（`routes/inventory.js`）
- [ ] 备份脚本 + crontab + 恢复演练（老胡另行规划）
- [ ] `.env` 改 `TRUST_PROXY=1` 并验证反代 XFF（部署侧，老胡定）——代码双桶后非必须
- [ ] `.env` 改 `COOKIE_SECURE=true`（前提：HTTPS 已生效，部署侧）
- [ ] 加 4 行安全响应头（可选非阻塞项）

做完前 5 条（约半天工作量）我认为就可以放心收费了。

---

## 五、我没动任何代码

本轮是**只读审查 + 实测**。唯一副作用是测试期间创建过 `data/` 目录和临时租户，
已全部删除，`git status` 恢复原状（5 个改动文件 + 2 份审计报告）。

所有实测结论都基于真实跑起来的服务，行号可直接跳转核实。

---

**说明**：本报告与 `SECURITY_AUDIT_2026-09-07.md`、`SECURITY_AUDIT_2026-09-08.md` 是互补关系。
前两轮解决的是"代码写得对不对"，本轮回答的是"能不能对外收钱"——
结论是**内核过关，运营层面的三块短板补齐就能上**。
